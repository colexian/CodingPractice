# -*- coding: utf-8 -*-
"""
Created on Wed Aug 31 20:15:21 2022

@author: ColeX
"""


def modlist(number: int) -> list:
    numbers = []
    for i in range(number):
        newnumb = int(input(f"item {i+1}:"))
        numbers.append(newnumb)
    return(numbers)


print(modlist(3))
